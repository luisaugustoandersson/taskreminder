<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="app.activity"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:label="@string/app_name" >
        <activity android:name="TaskReminder"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ListReminder"
                  android:label="TaskReminder"/>
        <activity android:name="CreateReminder"
                  android:label="TaskReminder"/>
        <activity android:name="ListNote"
                  android:label="TaskReminder"/>
        <activity android:name="CreateNote"
                  android:label="TaskReminder"/>
        <activity android:name="Register"
                  android:label="TaskReminder"/>
        <activity android:name="DescriptionNote"
                  android:label="TaskReminder"/>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest> 
